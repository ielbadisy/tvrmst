% Generated by hand for tvrmst
\name{survmat}
\alias{survmat}
\title{Construct a survmat Object}
\description{
Create a canonical survival matrix object with components \code{t} and \code{S}.
}
\usage{
survmat(t, S)
}
\arguments{
  \item{t}{Numeric time grid (strictly increasing).}
  \item{S}{Numeric matrix with \code{nrow(S) = length(t)}.}
}
\value{
A \code{survmat} object (list with components \code{t} and \code{S}).
}
\examples{
 t <- seq(0, 3, by = 1)
 S <- cbind(A = exp(-0.2 * t), B = exp(-0.3 * t))
 sm <- survmat(t, S)
}
